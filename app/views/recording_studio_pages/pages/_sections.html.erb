<% @rendered_sections.each_with_index do |rendered, index| %>
  <%= render "recording_studio_pages/pages/section",
             rendered: rendered,
             overlay: RecordingStudioPages::MenuOverlay.overlay?(rendered, @rendered_sections[index + 1]) %>
<% end %>
